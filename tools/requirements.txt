# Dependencies for the out-of-band Overture extractor (tools/extract_overture.py)
# ONLY. These carry the heavy binary readers and are deliberately kept OUT of
# pipeline/requirements.txt so the pipeline stays pure-Python (Python 3.14 safe).
overturemaps
pyshp>=2.3

# openpyxl: reads the Fire Department's Emergency Networking .xlsx report exports
# for tools/build_publicsafety.py (Public Safety dashboard).
openpyxl>=3.1

# requests: HTTP client for tools/build_address_points.py (Census batch geocoder).
# Floor at 2.33: earlier 2.32.x releases carry open CVEs (Dependabot #17/#19).
requests>=2.33
